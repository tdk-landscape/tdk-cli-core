# Tasks

## 1. Preflight checks

- [x] 1.1 ~~Confirm whether `dc_resource(..., auto_init=False)` pre-creates the underlying container~~ — **Done, confirmed empirically 2026-09-26** against the installed Tilt `v0.37.7`: a throwaway `dc_resource(auto_init=False)` produces no container object at all (`docker ps -a` empty for it, `uiresource.status.runtimeStatus`/`updateStatus` stay `none`) until triggered; `tilt trigger <name>` then creates and starts it in one call. Recorded under design D2/D3. No further verification needed on this point.
- [ ] 1.2 Confirm whether the vendored `sablierapp/sablier` version (`1.18.0`, per `traefik_standalone.star`) supports a webhook/hook callable on container start, or only exposes its own blocking-wait HTTP behavior. Verify by reading that version's docs/config reference and recording the result under design D3; if no hook exists (expected, since D4 needs a sidecar regardless), confirm the sidecar approach covers both D3's and D4's needs with one component.
- [x] 1.3 ~~Confirm the Tilt HTTP trigger API request shape~~ — **Superseded, 2026-09-26**: design D3 now prefers shelling out to the `tilt trigger <resource>` CLI (proven working end-to-end in the 1.1 probe) over the undocumented internal HTTP API, so no request-shape verification is needed. Confirm instead that `tilt trigger` accepts multiple resource names in one invocation for the dependency-group case (task 4.x); if it only accepts one, the wake path loops one call per group member instead.
- [ ] 1.4 Confirm how Traefik's Docker provider and file provider behave when both eventually define a router for the same resource name (D2's Docker-discovered router appearing once the container exists, alongside D4's static file-provider router). Verify by generating both for a test resource, starting Traefik, and checking `GET /api/http/routers` for duplicate-name conflicts or silent overrides; record the result (and, if needed, a naming convention to keep them distinct) under design D4.

## 2. Manifest schema (D1)

- [ ] 2.1 Add `deferStart` (boolean, default `false`) to the `sablier` schema block in `engine/topologies/tilt/manifest/schema.star` and to the `sablier` type in `cli/src/types/index.ts`. Verify with a schema validation test asserting a manifest with `sablier: {enable: true, deferStart: true}` passes validation and one with `deferStart` set but `enable` absent/false fails with a clear error (deferStart requires enable).
- [ ] 2.2 Update the `sablier` entry in `cli/src/utils/resource-features.ts` to document `deferStart`. Verify by running the existing feature-description test/lint that covers this file, if any, or a manual `tdk resource --help`-style check that the new field appears.

## 3. Per-resource deferred auto_init (D1)

- [ ] 3.1 Add a `resource_defers_start(manifest)` helper (co-located with the other manifest-derived helpers used by `apply_compose_resource_registration.star`) that reads the resolved `sablier.enable`/`sablier.deferStart` for a manifest. Verify with a unit test covering: no `sablier` block, `enable: false`, `enable: true` without `deferStart`, and `enable: true, deferStart: true`.
- [ ] 3.2 Change `_register_replicas` (`engine/topologies/tilt/resources/orchestrator/apply_compose_resource_registration.star:117`) to resolve `auto_init` per-resource as `auto_init_apps and not resource_defers_start(manifest)` instead of using `auto_init_apps` directly, keeping every other call site (config-gen, docker_build, migrators) on its existing flag. Verify with a generated-Tiltfile test asserting a `deferStart` resource's `dc_resource` has `auto_init=False` while its config-gen/docker_build resources keep `auto_init=True`, and a non-opted-in resource in the same landscape still gets `auto_init=True`.
- [ ] 3.3 Confirm end to end that a `deferStart` resource genuinely produces no container object after `tdk up` finishes (per 1.1's confirmed finding, this is expected, not a bug) — `docker ps -a` shows nothing for it — and that its config-gen and image build still completed. Verify by running `tdk up` against a test project with one `deferStart` resource and asserting both conditions.

## 4. Static route for never-created resources (D2, D4)

- [ ] 4.1 Add `--providers.file.directory=/etc/traefik/dynamic` to the standalone Traefik command list in `engine/topologies/platform/docker/compose/traefik_standalone.star`, plus a mounted directory for generated dynamic-config files. Verify with a generated-compose test asserting the flag and volume mount are present, and that Traefik starts successfully with an empty dynamic directory (no regression for landscapes with no `deferStart` resources).
- [ ] 4.2 Generate a file-provider dynamic-config entry for each `deferStart` resource, reusing the exact same router rule/service/middleware values the Docker-label path already computes for that resource (`traefik.star`'s `get_backend_traefik_labels`/`get_frontend_traefik_labels`), pointed at the wake sidecar's address instead of the resource's own port. Verify with a snapshot test comparing the generated file-provider entry's routing rule against the Docker-label version for the same manifest, asserting they match.
- [ ] 4.3 Stand up the wake sidecar referenced by 4.2 (a small always-on service, likely co-located with the existing `sablier` container) that: on a request for a `deferStart` resource with no running container, shells out to `tilt trigger` (per D3/D5) for the resource and its wake group, then holds the request and proxies it through once the resource's healthcheck passes, or returns a timeout error past its configured duration. Verify with an integration test: request a `deferStart` resource's route with no container running, assert the request is held then answered with the real backend response, and assert `docker ps` shows the container running afterward.
- [ ] 4.4 Apply task 1.4's finding: if Traefik conflicts or silently prefers one router when both the file-provider and Docker-discovered routes exist for the same resource, adjust naming/priority so the file-provider route only wins while no container exists. Verify with an integration test: after 4.3 wakes a resource once, request its route again and assert Traefik now answers via the Docker-discovered router (e.g. by checking Traefik's router API reports the docker-provider router as the one serving the request), not the file-provider one.

## 5. Wake path: Sablier discovery, dependency grouping, trigger call (D2, D3, D5)

- [ ] 5.1 In `tdk-cli-extensions/premium/networking/sablier_container_cycle.star`, change `_sablier_config`'s group fallback: when a manifest's `sablier.group` is unset, compute the group from the resource's own dependency chain (same data as `should_wait_for_dependencies` in `engine/lifecycle/orchestrator.star`) instead of falling back to `stack`. Verify with a unit test: a resource with an explicit `group` keeps it unchanged; a resource with no `group` and a database+NATS dependency gets a group containing those dependencies; a resource with no `group` and no dependencies gets a singleton group.
- [ ] 5.2 For a resource that has previously run and is now stopped/exited (a container object exists), keep Sablier's existing Docker-level start as the wake mechanism, and additionally fire the same `tilt trigger` call as 4.3 in parallel so Tilt's own state (e.g. a migrator that needs to re-run) catches up. Verify with an integration test: stop a previously-started test resource's container directly (`docker stop`), request its route, and assert the container restarts (Sablier's existing path) and `tilt trigger` was also called for it.
- [ ] 5.3 Confirm the held request in 4.3/5.2 is answered only once the resource is healthy, and fails with a clear timeout error past the resource's `sessionDuration`/timeout instead of hanging. Verify with an integration test using an artificially short timeout on a resource that never becomes healthy, asserting a timeout error rather than a hang.

## 6. tdk up / tdk doctor reporting (D6)

- [ ] 6.1 Add a "deferred" resource classification to `tdk up`'s summary output, sourced from the resolved `sablier.enable && sablier.deferStart` per manifest (not from container state). Verify with a `tdk up` run against a landscape containing a `deferStart` resource, asserting the summary lists it as deferred rather than omitting it or listing it as pending.
- [ ] 6.2 Make `tdk doctor` skip its usual "not running" / "unhealthy" flags for a resource classified as deferred and never yet triggered, per `doctor-runtime.ts`. Verify with a `tdk doctor` run against the same landscape, asserting no failing check is reported solely because the deferred resource hasn't started, and that a genuinely deferred-but-broken resource (triggered, past its timeout, still unhealthy) is still flagged once it has actually been triggered.

## 7. Documentation and integration

- [ ] 7.1 Document `deferStart` in the same place the `sablier` block itself is documented (existing premium feature docs), including the dependency-group default from D5, the two wake paths from D2/D3/D4, and the new Traefik file-provider requirement from D4. Verify the documented example manifest and commands run as written.
- [ ] 7.2 Re-vendor into `tdk-erp-system` (`tdk project --yes`, touch the Tiltfile, rebuild affected images), set `sablier: {enable: true, deferStart: true}` on `data-governance-api`'s `service.json`, and run a full `tdk up`. Verify that `data-governance-api` does not start during the initial bring-up, that `curl http://api.tdk-erp-system.localhost/api/data-governance/health` while the rest of the landscape is still starting returns a real (not `404`/connection-refused) health response within the resource's normal cold-start time — directly closing the gap reproduced live during design (see design.md Context) — and that `tdk doctor` does not flag it before that request and does not flag it as broken after.
