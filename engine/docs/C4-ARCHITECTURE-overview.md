# TDK Landscape Tilt Architecture - C4 Model

## Level 1: System Context

```mermaid
graph TB
    Dev[Developer]
    Tilt[Tilt Orchestrator]
    Docker[Docker/OrbStack]
    Git[Git Repository]
    Services[Microservices<br/>30+ services]
    Infra[Infrastructure<br/>Postgres, NATS, Traefik]
    
    Dev -->|tilt up| Tilt
    Tilt -->|watches| Git
    Tilt -->|builds| Docker
    Tilt -->|orchestrates| Services
    Tilt -->|provisions| Infra
    Docker -->|runs| Services
    Docker -->|runs| Infra
    Services -->|uses| Infra
    
    style Tilt fill:#f9f,stroke:#333,stroke-width:4px
    style Dev fill:#bbf,stroke:#333,stroke-width:2px
    style Services fill:#bfb,stroke:#333,stroke-width:2px
    style Infra fill:#fbb,stroke:#333,stroke-width:2px
```

**Purpose**: Tilt automates the entire local development lifecycle - from code changes to running containers.

---

## Level 2: Container Diagram - Tilt SDK Architecture

```mermaid
graph TB
    subgraph "Developer Interface"
        CLI[Tiltfile<br/>Main Entrypoint]
        Focus[Focus Mode<br/>--focus=user]
    end
    
    subgraph "Core Layer"
        Utils[Utils Module<br/>I/O, validation, helpers]
        Manifest[Manifest Module<br/>JSON loading & validation]
        Registry[Service Registry<br/>APP_RESOURCES definition]
        DepSync[Dependency Sync<br/>package.json → configs]
    end
    
    subgraph "Provider Layer - Config Generators"
        Docker[Docker Provider<br/>Dockerfile generation]
        TS[TypeScript Provider<br/>tsconfig.json]
        Vite[Vite Provider<br/>vite.config.ts]
        NPM[Package Config<br/>.npmrc, bunfig.toml]
    end
    
    subgraph "Lifecycle Layer - Orchestration"
        Libs[Library Manager<br/>Shared packages]
        DB[Database Provider<br/>Per-service DBs]
        Orch[Orchestrator<br/>Service coordination]
        Watch[Watchers<br/>File change detection]
    end
    
    subgraph "Infrastructure"
        Golden[Golden Image Builder<br/>Base Docker images]
        Network[Network Manager<br/>Docker network repair]
        Infisical[Secrets Validator<br/>Infisical integration]
    end
    
    CLI --> Focus
    CLI --> Utils
    Focus --> Registry
    Utils --> Manifest
    Manifest --> Registry
    Registry --> DepSync
    
    DepSync --> Docker
    DepSync --> TS
    DepSync --> Vite
    DepSync --> NPM
    
    Docker --> Libs
    TS --> Libs
    Vite --> Libs
    NPM --> Libs
    
    Libs --> DB
    DB --> Orch
    Orch --> Watch
    
    Orch --> Golden
    Orch --> Network
    Orch --> Infisical
    
    style CLI fill:#f9f,stroke:#333,stroke-width:4px
    style Registry fill:#ff9,stroke:#333,stroke-width:3px
    style Docker fill:#9cf,stroke:#333,stroke-width:3px
    style Orch fill:#9f9,stroke:#333,stroke-width:3px
```

---

## Level 3: Component Diagram - Docker Build Pipeline

```mermaid
graph LR
    subgraph "Input Detection"
        Manifest[Service Manifest<br/>JSON file]
        PkgJSON[package.json<br/>Dependencies]
        Code[Source Code<br/>src/]
    end
    
    subgraph "Golden Image System"
        L1Base[L1: OS Base<br/>Alpine + tools]
        L2Deps[L2: Dependencies<br/>Bun + packages]
        L4Runtime[L4: Runtime<br/>Backend/Frontend/Migrator]
    end
    
    subgraph "Service-Specific Layers"
        L3Build[L3: Build Layer<br/>TypeScript compilation]
        L4Service[L4: Service Runtime<br/>Final container]
    end
    
    subgraph "Output Artifacts"
        DockerfileApp[Dockerfile.app.autogenerated]
        DockerfileMigrator[Dockerfile.migrator.autogenerated]
        Compose[docker-compose.autogenerated.yml]
    end
    
    subgraph "Orchestration"
        TiltResource[Tilt Resource<br/>service-backend]
        DockerBuild[Docker Build<br/>Multi-stage]
        Container[Running Container<br/>Port 4000]
    end
    
    Manifest --> L1Base
    PkgJSON --> L2Deps
    Code --> L3Build
    
    L1Base --> L2Deps
    L2Deps --> L3Build
    L3Build --> L4Service
    L4Runtime --> L4Service
    
    L4Service --> DockerfileApp
    L4Service --> DockerfileMigrator
    DockerfileApp --> Compose
    DockerfileMigrator --> Compose
    
    Compose --> TiltResource
    TiltResource --> DockerBuild
    DockerBuild --> Container
    
    style L1Base fill:#faa,stroke:#333,stroke-width:2px
    style L2Deps fill:#fca,stroke:#333,stroke-width:2px
    style L3Build fill:#fea,stroke:#333,stroke-width:2px
    style L4Service fill:#afa,stroke:#333,stroke-width:2px
    style Container fill:#9f9,stroke:#333,stroke-width:3px
```

---

## Level 4A: Code Flow - Manifest-Driven Resource Creation

```mermaid
sequenceDiagram
    participant Tilt as Tilt Orchestrator
    participant Manifest as service.json
    participant ConfigGen as {service}-config-gen (Tilt Resource)
    participant Generators as 10+ Config Generators
    participant Docker as Docker Build
    
    Tilt->>Manifest: 1. Scan & Auto-discover
    Manifest->>ConfigGen: 2. Create per-manifest resource
    Note over ConfigGen: Resource name: user-backend-config-gen<br/>Label: config-gen, user<br/>Deps: manifest.json, package.json
    
    ConfigGen->>Generators: 3. Trigger all generators for THIS manifest
    Note over Generators: tsconfig.json (Docker)<br/>tsconfig.json (local)<br/>vite.config.ts<br/>Dockerfile.app.autogenerated<br/>.dockerignore<br/>.env.autogenerated<br/>.npmrc (Docker)<br/>.npmrc (local)<br/>bunfig.toml (Docker)<br/>bunfig.toml (local)<br/>nginx.conf (if frontend)<br/>prisma.config.ts (if backend)
    
    Generators-->>ConfigGen: 4. Configs generated
    ConfigGen->>Docker: 5. Trigger Docker build (depends on config-gen)
    Docker-->>Tilt: 6. Service running
    
    Note over Manifest,Docker: ✅ Manifest-driven: 1 manifest → 1 resource → 10 generators<br/>❌ NOT bulk: 100 manifests → 1 loop → 1000 configs
```

### Architecture Evolution

**Before (Bulk Generation)** - Lines 112-251 of apply.star:
```starlark
for resource in ALL_RESOURCES:  # 30 services
    generate_vite(resource)      # Loop 1
for resource in ALL_RESOURCES:
    generate_dockerfile(resource) # Loop 2
for resource in ALL_RESOURCES:
    generate_tsconfig(resource)   # Loop 3
# ... 7 more loops

# ❌ Problems:
# - No per-resource tracking in Tilt UI
# - Change 1 manifest → regenerate ALL configs
# - Can't see which config belongs to which service
```

**After (Manifest-Driven)** - Lines 114-136 of apply.star:
```starlark
for resource in resource_resources:
    manifest = load_manifest(resource)
    
    # Create ONE Tilt resource that generates ALL configs for THIS service
    config_gen_resource = ManifestResource.create_config_resource(
        resource_name, resource, manifest, backend_manifest, ctx
    )
    # Result: user-management-backend-config-gen (visible in Tilt UI)

# ✅ Benefits:
# - 30 granular Tilt resources (one per service)
# - Change 1 manifest → regenerate 10 configs for THAT service only  
# - Clear dependency: Docker build depends on config-gen resource
```

### Tilt UI Visibility

In Tilt dashboard you now see per-manifest resources:

```
📁 config-gen (label)
  ├─ user-management-backend-config-gen ✅
  ├─ user-management-frontend-config-gen ✅
  ├─ identity-management-backend-config-gen ✅
  ├─ identity-management-frontend-config-gen ✅
  └─ staff-management-backend-config-gen ✅

📁 user (label)
  ├─ user-management-backend-config-gen ⬆️ (dependency)
  ├─ user-management-backend ✅ (depends on config-gen)
  ├─ user-management-frontend-config-gen ⬆️ (dependency)
  └─ user-management-frontend ✅ (depends on config-gen)
```

### Dependency Chain

```
manifest.json  ─┐
package.json   ─┼─→ {service}-config-gen ───→ {service} (Docker)
                │    (10+ generators)           (depends on configs)
                │
Infrastructure ─┘
  golden-image-l1 → golden-image-l2 → verdaccio → postgres
```

---

## Level 4B: Code Flow - Dependency Synchronization

```mermaid
sequenceDiagram
    participant Dev as Developer
    participant File as package.json
    participant Tilt as Tilt Watcher
    participant DepSync as Dependency Sync
    participant TS as TSConfig Generator
    participant Vite as Vite Generator
    participant Docker as Docker Generator
    participant Service as Service Container
    
    Dev->>File: bun add @tdk-landscape/eventing
    File->>Tilt: File change detected
    Tilt->>DepSync: Trigger sync
    DepSync->>DepSync: Extract internal deps
    DepSync->>DepSync: Resolve library paths
    DepSync->>TS: Generate tsconfig.json paths
    DepSync->>Vite: Generate vite.config.ts aliases
    DepSync->>Docker: Update COPY statements
    Docker->>Docker: Regenerate Dockerfile
    Docker->>Service: Rebuild container
    Service-->>Dev: Service restarted with new dependency
    
    Note over DepSync,Docker: Convention over Configuration:<br/>@tdk-landscape/platform-x → platform/packages/platform-x/src
```

---

